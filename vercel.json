{
    "buildCommand": "npm run build --workspace=@zenwallet/merchant-dashboard",
    "outputDirectory": "apps/merchant-dashboard/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}